server:
  port: 8080

spring:
  application:
    name: spring-cicd-k8s-demo

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always

app:
  version: "v2"
